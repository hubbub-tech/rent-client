export const RentalsCancelLogisticsButton = ({ logisticsId }) => {
  return (
    <button
      type="button"
      className="btn btn-danger btn-sm my-1 d-flex justify-content-end"
    >
      Cancel
    </button>
  );
}
